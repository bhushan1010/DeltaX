{
  "name": "deltax",
  "version": "1.0.0",
  "description": "DeltaX Project Management Platform",
  "private": true,
  "engines": {
    "node": ">=20.0.0"
  },
  "scripts": {
    "install:all": "npm install --prefix apps/api && npm install --legacy-peer-deps --prefix apps/web",
    "build": "npm run build --prefix apps/api && npm run build --prefix apps/web",
    "start": "npm start --prefix apps/api",
    "dev:api": "npm run dev --prefix apps/api",
    "dev:web": "npm run dev --prefix apps/web",
    "test": "npm test --prefix apps/api && npm test --prefix apps/web"
  },
  "keywords": [],
  "author": "",
  "license": "ISC"
}